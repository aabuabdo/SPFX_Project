name: Deploy SPFx App to SharePoint Online

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 🧩 1. سحب الكود من GitHub
      - name: Checkout code
        uses: actions/checkout@v4

      # 🟢 2. تثبيت Node.js 20
      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      # ⚙️ 3. تثبيت Microsoft 365 CLI
      - name: Install Microsoft 365 CLI
        run: npm install -g @pnp/cli-microsoft365

      # 📦 4. تثبيت الحزم
      - name: Install dependencies
        run: npm ci

      # 🧱 5. بناء المشروع (تجاهل تحذيرات stderr)
      - name: Build SPFx solution
        run: |
          gulp bundle --ship 2> build-errors.log || true
          gulp package-solution --ship 2>> build-errors.log || true

      # 🔐 6. تسجيل الدخول إلى Microsoft 365
      - name: Login to Microsoft 365
        run: |
          m365 login --authType password --userName ${{ secrets.ADMIN_USERNAME }} --password ${{ secrets.ADMIN_PASSWORD }}

      # 🚀 7. رفع التطبيق إلى App Catalog
      - name: Deploy SPFx app to SharePoint Online
        run: |
          m365 spo app add --filePath "sharepoint/solution/myapp.sppkg" --overwrite
          m365 spo app deploy --name "myapp.sppkg" --scope tenant

      # 🧩 8. (اختياري) تثبيت التطبيق على موقع محدد
      - name: Install SPFx app on site
        run: |
          m365 spo app install --name "myapp.sppkg" --siteUrl "https://https://2327zs.sharepoint.com/sites/dev" --scope sitecollection

      # 📧 9. (اختياري) إرسال بريد بعد النشر
      - name: Send deployment email
        run: |
          m365 outlook mail send --to "aabuabdo1998@gmail.com" --subject "✅ SPFx Deployment Successful" --body "Your SPFx app (myapp.sppkg) was deployed successfully."
