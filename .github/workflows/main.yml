name: SPFx CI/CD Pipeline 🚀

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: windows-latest

    steps:
      # 🧩 1. سحب الكود
      - name: Checkout repository
        uses: actions/checkout@v4

      # ⚙️ 2. إعداد Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 📦 3. تثبيت الحزم
      - name: Install dependencies
        working-directory: .
        run: npm install

      # 🏗️ 4. بناء المشروع SPFx
      - name: Build SPFx solution
        working-directory: .
        run: |
          try {
            gulp bundle --ship
          } catch {
            Write-Host "⚠️ Ignoring lint warnings"
          }

      # 📦 5. إنشاء الحزمة
      - name: Package SPFx solution
        working-directory: .
        run: |
          try {
            gulp package-solution --ship
          } catch {
            Write-Host "⚠️ Ignoring lint warnings"
          }

      # 🔍 6. التحقق من وجود ملف sppkg
      - name: Verify SPFx package exists
        working-directory: ./sharepoint/solution
        run: |
          echo "📦 Checking for SPFx package..."
          if (!(Test-Path *.sppkg)) {
            Write-Host "❌ No SPFx package found!"; exit 1
          } else {
            Write-Host "✅ SPFx package ready for deployment."
          }

      # 🧰 7. تثبيت CLI for Microsoft 365
      - name: Install CLI for Microsoft 365
        run: npm install -g @pnp/cli-microsoft365

      # 🔐 8. تسجيل الدخول
      - name: Login to Microsoft 365
        run: >
          m365 login --authType secret
          --tenant ${{ secrets.TENANT_ID }}
          --clientId ${{ secrets.CLIENT_ID }}
          --clientSecret ${{ secrets.CLIENT_SECRET }}

      # 🚀 9. نشر الحزمة إلى App Catalog
      - name: Upload and Deploy SPFx Package
        run: |
          $package = Get-ChildItem "sharepoint/solution/*.sppkg" | Select-Object -ExpandProperty FullName
          echo "🚀 Uploading $package to App Catalog..."
          m365 spo app add --filePath "$package" --appCatalogScope tenant --overwrite
          $name = Split-Path $package -Leaf
          echo "📦 Deploying $name..."
          m365 spo app deploy --name "$name" --appCatalogScope tenant
